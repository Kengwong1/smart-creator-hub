import streamlit as st
import os
from dotenv import load_dotenv
import google.generativeai as genai

# --- 1. การตั้งค่าหน้าเว็บ ---
st.set_page_config(page_title="Smart Creator Hub v8.0", page_icon="🎬", layout="centered")
load_dotenv()

# --- 2. ระบบ AI (สมองกลสำหรับทุกคอนเทนต์) ---
def generate_all_in_one_content(topic):
    keys = st.secrets.get("GEMINI_KEYS", [])
    # ปรับคำสั่งให้เป็นกลางสำหรับทุกคอนเทนต์
    instruction = (
        f"คุณคือผู้เชี่ยวชาญด้าน Content Marketing สำหรับทุกแพลตฟอร์ม "
        f"กรุณาช่วยวางแผนคอนเทนต์สำหรับหัวข้อ: '{topic}' โดยให้รายละเอียดดังนี้:\n"
        f"1. ชื่อคลิปดึงดูดใจ (Viral Titles) 5 แบบ\n"
        f"2. แคปชั่นสำหรับโพสต์ (Social Media Captions) พร้อมติด Hashtag และจุดป้ายยา\n"
        f"3. สคริปต์วิดีโอ (Video Script) แบ่งเป็นลำดับเวลาอย่างละเอียด\n"
        f"โปรดตอบเป็นภาษาไทยที่เป็นกันเองและดูเป็นมืออาชีพ"
    )
    
    for key in keys:
        try:
            genai.configure(api_key=key)
            model = genai.GenerativeModel('gemini-flash-latest')
            res = model.generate_content(instruction)
            return res.text
        except:
            continue
    return "QUOTA_FULL"

# --- 3. Sidebar เมนู ---
with st.sidebar:
    st.title("🎬 Smart Creator Hub v8.0")
    st.write("เครื่องมือผลิตคอนเทนต์สำหรับทุกคน ✨")
    menu = st.radio(
        "เมนูการใช้งาน:", 
        ["🚀 สร้างคอนเทนต์ใหม่", "📝 วางแผนสคริปต์", "💰 เขียนแคปชั่นป้ายยา", "🔍 ตั้งชื่อคลิป Viral"]
    )
    st.divider()
    st.caption("v8.0 | Pure Content Focus")

# --- 4. โซนการทำงาน ---

if menu == "🚀 สร้างคอนเทนต์ใหม่":
    st.header("🚀 สร้างคอนเทนต์แบบครบวงจร")
    st.write("ไม่ว่าคุณจะทำคอนเทนต์เรื่องอะไร ระบบจะช่วยคิดแผนให้ทันทีค่ะ")
    
    topic = st.text_input("คุณอยากทำคอนเทนต์เรื่องอะไร?", placeholder="เช่น รีวิวร้านอาหาร, สอนใช้แอป, ทริปท่องเที่ยว...")

    if st.button("✨ เริ่มผลิตเนื้อหา"):
        if not topic:
            st.warning("กรุณาใส่หัวข้อที่คุณต้องการทำคอนเทนต์ค่ะ")
        else:
            with st.spinner("⏳ AI กำลังออกแบบคอนเทนต์ให้คุณ..."):
                res = generate_all_in_one_content(topic)
                if res == "QUOTA_FULL":
                    st.error("ขณะนี้โควต้าเต็ม รบกวนรอประมาณ 1 นาทีแล้วลองใหม่นะค่ะ")
                else:
                    st.divider()
                    st.subheader("📝 ผลลัพธ์คอนเทนต์ของคุณ")
                    st.markdown(res)
                    st.success("คุณสามารถก๊อปปี้เนื้อหาไปใช้งานได้ทันทีค่ะ")

elif menu == "📝 วางแผนสคริปต์":
    st.header("📝 วางแผนสคริปต์อย่างละเอียด")
    detail_topic = st.text_area("หัวข้อหรือรายละเอียดคอนเทนต์:")
    if st.button("สร้างสคริปต์"):
        res = generate_all_in_one_content(f"ขอสคริปต์วิดีโออย่างเดียวสำหรับเรื่อง: {detail_topic}")
        st.markdown(res)

elif menu == "💰 เขียนแคปชั่นป้ายยา":
    st.header("💰 เขียนแคปชั่นป้ายยา")
    prod = st.text_input("ชื่อสินค้าหรือบริการ:")
    if st.button("สร้างแคปชั่น"):
        res = generate_all_in_one_content(f"เขียนแคปชั่นขายของ/ป้ายยา สำหรับ: {prod}")
        st.code(res)

elif menu == "🔍 ตั้งชื่อคลิป Viral":
    st.header("🔍 ตั้งชื่อคลิปให้น่ากดดู")
    v_topic = st.text_input("คลิปนี้เกี่ยวกับอะไร:")
    if st.button("คิดชื่อคลิป"):
        res = generate_all_in_one_content(f"ขอชื่อคลิปที่คนเห็นแล้วต้องกดดู 10 แบบ สำหรับ: {v_topic}")
        st.markdown(res)