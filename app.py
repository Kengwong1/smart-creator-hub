import streamlit as st
import os
from dotenv import load_dotenv
import google.generativeai as genai
import time
import urllib.parse

# --- 1. การตั้งค่าหน้าเว็บ ---
st.set_page_config(page_title="Smart Creator Hub", page_icon="🎬", layout="wide")
load_dotenv()

# --- 2. ฟังก์ชันเรียกใช้ Gemini พร้อมระบบสลับกุญแจ (Rotation) ---
def call_gemini_with_retry(prompt_text):
    # ดึงรายการกุญแจจาก Secrets
    keys = st.secrets.get("GEMINI_KEYS", [])
    if not keys:
        st.error("❌ ไม่พบรายการ GEMINI_KEYS ใน Secrets ค่ะ")
        st.stop()
    
    for idx, key in enumerate(keys):
        try:
            genai.configure(api_key=key)
            model = genai.GenerativeModel('gemini-flash-latest')
            response = model.generate_content(prompt_text)
            return response.text
        except Exception as e:
            # ถ้าโควต้าเต็ม (429) ให้ลองกุญแจดอกถัดไป
            if "429" in str(e) or "ResourceExhausted" in str(e):
                if idx < len(keys) - 1:
                    time.sleep(2)
                    continue
                else:
                    st.error("❌ กุญแจทุกดอกโควต้าเต็มหมดแล้วค่ะ รบกวนพัก 1-2 นาทีนะคะ")
                    st.stop()
    return None

# --- 3. ฟังก์ชันเสกรูป (ใช้ Pollinations AI - เสถียรและเร็วที่สุด) ---
def generate_image_url(prompt, width, height):
    # เข้ารหัสข้อความเพื่อส่งผ่าน URL
    encoded_prompt = urllib.parse.quote(prompt)
    # ใช้ Seed จากเวลาปัจจุบันเพื่อให้รูปไม่ซ้ำกัน
    seed = int(time.time())
    image_url = f"https://pollinations.ai/p/{encoded_prompt}?width={width}&height={height}&seed={seed}&model=flux"
    return image_url

# --- 4. Sidebar เมนู ---
with st.sidebar:
    st.title("🎬 Smart Creator Hub")
    st.write(f"สวัสดีค่ะคุณเก่ง ✨")
    menu = st.radio(
        "เลือกเครื่องมือ:",
        ["🎨 เสกรูปภาพด้วย AI", "🎬 วางแผนคอนเทนต์", "💰 เขียนแคปชั่นป้ายยา", "🔍 ตั้งชื่อคลิปให้น่าคลิก", "💬 ผู้ช่วยตอบคอมเมนต์"]
    )
    st.divider()
    st.caption("v3.0 | Stable & Reliable")

# --- 5. โซนการทำงานตามเมนู ---

# --- เมนู: เสกรูปภาพ ---
if menu == "🎨 เสกรูปภาพด้วย AI":
    st.header("🎨 AI ศิลปินเสกรูปภาพ (ระบบเสถียร 100%)")
    img_desc = st.text_area("อยากให้ AI วาดภาพอะไร? (พิมพ์ไทยหรืออังกฤษก็ได้)", placeholder="เช่น นกยูงรำแพนกลางสวนดอกไม้ สไตล์ภาพถ่ายจริง")
    
    col1, col2 = st.columns(2)
    with col1:
        size_option = st.selectbox(
            "เลือกขนาดภาพ:",
            ["แนวตั้ง (9:16) - TikTok/Reels", "แนวนอน (16:9) - FB/YouTube", "จัตุรัส (1:1) - IG/Profile"]
        )
    
    # กำหนดขนาด Width/Height
    if "9:16" in size_option: w, h = 540, 960
    elif "16:9" in size_option: w, h = 960, 540
    else: w, h = 768, 768

    if st.button("✨ เริ่มเสกรูป"):
        if not img_desc:
            st.warning("กรุณาใส่คำบรรยายภาพก่อนนะคะ")
        else:
            with st.spinner("⏳ กำลังเตรียมคำสั่งภาพระดับโปร..."):
                # ตรวจสอบว่าเป็นอังกฤษอยู่แล้วหรือไม่
                is_english = all(ord(c) < 128 for c in img_desc[:50])
                if is_english:
                    eng_prompt = img_desc
                else:
                    eng_prompt = call_gemini_with_retry(f"Write a short, high-quality English image prompt for: {img_desc}")
            
            if eng_prompt:
                st.info(f"✅ กำลังวาด: {eng_prompt[:100]}...")
                final_url = generate_image_url(eng_prompt, w, h)
                
                # แสดงรูปภาพจาก URL
                st.image(final_url, caption=f"เสร็จแล้วค่ะ! วาดโดย Pollinations AI", use_container_width=True)
                
                # ปุ่มดาวน์โหลด (แก้ไข Error ที่เกิดขึ้นเรียบร้อยแล้ว)
                st.markdown(f'### [📥 คลิกที่นี่เพื่อดาวน์โหลดรูปภาพ]({final_url})')

# --- เมนู: วางแผนคอนเทนต์ ---
elif menu == "🎬 วางแผนคอนเทนต์":
    st.header("🎬 วางแผนคอนเทนต์ทุกแพลตฟอร์ม")
    topic = st.text_input("หัวข้อคอนเทนต์")
    if st.button("✨ วางแผน"):
        with st.spinner("กำลังคิดแผน..."):
            result = call_gemini_with_retry(f"วางแผนคอนเทนต์เรื่อง {topic} ให้ละเอียดและน่าสนใจ")
            if result: st.markdown(result)

# --- เมนู: เขียนแคปชั่นป้ายยา ---
elif menu == "💰 เขียนแคปชั่นป้ายยา":
    st.header("💰 เขียนแคปชั่นป้ายยา Affiliate")
    details = st.text_area("ข้อมูลสินค้า")
    if st.button("💸 เสกแคปชั่น"):
        with st.spinner("กำลังเรียบเรียง..."):
            result = call_gemini_with_retry(f"เขียนแคปชั่นป้ายยาแรงๆ จากข้อมูลนี้: {details}")
            if result: st.code(result)

# --- เมนู: ตั้งชื่อคลิปให้น่าคลิก ---
elif menu == "🔍 ตั้งชื่อคลิปให้น่าคลิก":
    st.header("🔍 ตัวช่วยตั้งชื่อให้คนกดดู")
    topic_name = st.text_input("เนื้อหาคลิปสรุป")
    if st.button("🚀 เสกชื่อคลิป"):
        with st.spinner("กำลังคิดชื่อ..."):
            result = call_gemini_with_retry(f"คิดชื่อคลิป Viral 5 แบบ สำหรับเรื่อง {topic_name}")
            if result: st.markdown(result)

# --- เมนู: ผู้ช่วยตอบคอมเมนต์ ---
elif menu == "💬 ผู้ช่วยตอบคอมเมนต์":
    st.header("💬 ผู้ช่วยตอบคอมเมนต์")
    comment = st.text_area("คอมเมนต์จากแฟนคลับ")
    style = st.select_slider("สไตล์", options=["สุภาพ", "เป็นกันเอง", "กวนๆ"])
    if st.button("💭 คิดคำตอบ"):
        with st.spinner("กำลังหาคำตอบ..."):
            result = call_gemini_with_retry(f"คิดคำตอบคอมเมนต์ '{comment}' ในสไตล์ {style}")
            if result: st.code(result)