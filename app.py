import streamlit as st
import sqlite3
import pandas as pd

# --- 1. จัดการฐานข้อมูล (สร้างไฟล์เก็บข้อมูลในเครื่อง) ---
conn = sqlite3.connect('merchant_data.db', check_same_thread=False)
c = conn.cursor()
c.execute('''CREATE TABLE IF NOT EXISTS products 
             (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, price REAL, link TEXT, category TEXT)''')
conn.commit()

# --- 2. การตั้งค่าหน้าเว็บ ---
st.set_page_config(page_title="Merchant Hub v9.0", layout="wide")

# --- 3. Sidebar เมนู ---
with st.sidebar:
    st.title("📦 Creator Hub v9.0")
    menu = st.radio("เมนูหลัก", ["➕ เพิ่มสินค้า/ลิงก์", "📋 ดูคลังสินค้าทั้งหมด", "📊 สรุปยอด/กำไร"])

# --- 4. โซนการทำงาน ---

# 4.1 เพิ่มสินค้า (ไม่ต้องใช้ API ฟรีตลอดกาล)
if menu == "➕ เพิ่มสินค้า/ลิงก์":
    st.header("➕ เพิ่มรายการสินค้าใหม่")
    with st.form("product_form"):
        p_name = st.text_input("ชื่อสินค้า (เช่น ขาตั้งกล้องรุ่น A)")
        p_price = st.number_input("ราคาสินค้า (บาท)", min_value=0.0)
        p_link = st.text_input("ลิงก์สินค้า (Affiliate Link)")
        p_cat = st.selectbox("หมวดหมู่", ["อุปกรณ์ไลฟ์สด", "เครื่องมือช่าง", "ของใช้ทั่วไป", "อื่นๆ"])
        submit = st.form_submit_button("บันทึกข้อมูล")
        
        if submit:
            if p_name and p_link:
                c.execute("INSERT INTO products (name, price, link, category) VALUES (?,?,?,?)", 
                          (p_name, p_price, p_link, p_cat))
                conn.commit()
                st.success(f"บันทึก {p_name} เรียบร้อยแล้วค่ะ!")
            else:
                st.error("กรุณากรอกข้อมูลให้ครบถ้วนนะค่ะ")

# 4.2 ดูคลังสินค้า (เอาไว้ออกรายการไปโพสต์)
elif menu == "📋 ดูคลังสินค้าทั้งหมด":
    st.header("📋 รายการสินค้าที่คุณบันทึกไว้")
    data = pd.read_sql_query("SELECT * FROM products", conn)
    
    if not data.empty:
        st.dataframe(data, use_container_width=True)
        # ฟีเจอร์เด็ด: ก๊อปปี้ไปโพสต์
        st.subheader("🔗 ก๊อปปี้ลิงก์ไปป้ายยา")
        for index, row in data.iterrows():
            st.code(f"🔥 {row['name']} \n💰 ราคา: {row['price']} บาท \n👉 พิกัด: {row['link']}")
    else:
        st.info("ยังไม่มีข้อมูลสินค้าค่ะ เริ่มบันทึกที่เมนูเพิ่มสินค้าได้เลยค่ะ")

# 4.3 ระบบสรุป (ตัวอย่าง)
elif menu == "📊 สรุปยอด/กำไร":
    st.header("📊 ระบบสรุปภาพรวม")
    st.write("ส่วนนี้ไว้ใช้คำนวณกำไรจากการขายหรือยอดคลิกในอนาคตค่ะ")