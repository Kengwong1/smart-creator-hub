import streamlit as st
import os
from dotenv import load_dotenv
import google.generativeai as genai

# 1. ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="Smart Creator Hub", page_icon="🎬", layout="wide")

# 2. โหลดกุญแจ
load_dotenv()
api_key = os.getenv("GEMINI_API_KEY")

if not api_key:
    st.error("❌ ไม่พบ API Key! กรุณาเช็กใน Advanced Settings ของ Streamlit นะคะ")
    st.stop()

genai.configure(api_key=api_key)
model = genai.GenerativeModel('gemini-flash-latest')

# --- Sidebar เมนูด้านซ้าย ---
with st.sidebar:
    st.title("🎬 Smart Creator Hub")
    menu = st.radio(
        "เลือกเครื่องมือที่ต้องการ:",
        ["🎬 คิดสคริปต์คลิป", "💰 เขียนแคปชั่นป้ายยา", "🎨 เจน Prompt วาดรูป", "🔧 วิเคราะห์งานซ่อม"]
    )
    st.divider()
    st.caption("พัฒนาโดยคุณเก่ง v1.1")

# --- โซน 1: คิดสคริปต์คลิป ---
if menu == "🎬 คิดสคริปต์คลิป":
    st.header("🎬 ผู้ช่วยคิดสคริปต์คลิปสั้น")
    product = st.text_input("ชื่อสินค้า/หัวข้อ")
    style = st.selectbox("สไตล์", ["ตลก", "จริงจัง", "เล่าเรื่อง"])
    if st.button("เสกสคริปต์"):
        prompt = f"เขียนสคริปต์ TikTok 30 วิ สำหรับ {product} สไตล์ {style} พร้อมบอกมุมกล้อง"
        res = model.generate_content(prompt)
        st.markdown(res.text)

# --- โซน 2: เขียนแคปชั่นป้ายยา ---
elif menu == "💰 เขียนแคปชั่นป้ายยา":
    st.header("💰 เขียนแคปชั่นป้ายยา Affiliate")
    details = st.text_area("ข้อมูลสินค้า")
    if st.button("เขียนแคปชั่น"):
        prompt = f"เขียนแคปชั่นป้ายยาแรงๆ สำหรับ {details} ใส่ Emoji และ Hashtag"
        res = model.generate_content(prompt)
        st.code(res.text)

# --- โซน 3: เจน Prompt วาดรูป (NEW!) ---
elif menu == "🎨 เจน Prompt วาดรูป":
    st.header("🎨 ตัวช่วยสร้าง Prompt สำหรับ AI วาดรูป")
    img_desc = st.text_input("อยากให้ภาพออกมาเป็นยังไง?", placeholder="เช่น หูฟังบลูทูธวางอยู่บนชายหาดตอนพระอาทิตย์ตก")
    if st.button("สร้าง Prompt ภาษาอังกฤษ"):
        prompt = f"ช่วยสร้าง AI Image Prompt ภาษาอังกฤษที่ละเอียดและสวยงาม สำหรับรูป: {img_desc}"
        res = model.generate_content(prompt)
        st.success("ก๊อปปี้ไปใช้ใน AI วาดรูปได้เลยค่ะ:")
        st.code(res.text)

# --- โซน 4: วิเคราะห์งานซ่อม (NEW!) ---
elif menu == "🔧 วิเคราะห์งานซ่อม":
    st.header("🔧 ผู้ช่วยวิเคราะห์อาการเสียและงานซ่อม")
    issue = st.text_area("อาการเสีย หรือรหัส Error", placeholder="เช่น มือถือ Samsung เปิดไม่ติด ชาร์จไฟไม่เข้า หรือ Error Code เครื่องซักผ้า")
    if st.button("วิเคราะห์ปัญหา"):
        prompt = f"ในฐานะช่างเทคนิคผู้เชี่ยวชาญ ช่วยวิเคราะห์สาเหตุและวิธีแก้ไขสำหรับอาการ: {issue} ขอเป็นข้อๆ เข้าใจง่าย"
        res = model.generate_content(prompt)
        st.info("บทวิเคราะห์เบื้องต้น:")
        st.markdown(res.text)