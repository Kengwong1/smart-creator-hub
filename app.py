import streamlit as st
import os
from dotenv import load_dotenv
import google.generativeai as genai

# 1. ตั้งค่าหน้าเว็บให้ดูเป็นมืออาชีพ
st.set_page_config(page_title="Smart Creator Hub", page_icon="🎬", layout="wide")

# 2. โหลดกุญแจ AI
load_dotenv()
api_key = os.getenv("GEMINI_API_KEY")

# ตรวจสอบกุญแจ
if not api_key:
    st.error("❌ ไม่พบ API Key! กรุณาตรวจสอบการตั้งค่า Secrets ใน Streamlit นะคะ")
    st.stop()

# ตั้งค่าโมเดล AI (ใช้รุ่นที่เสถียรและเร็วที่สุดสำหรับสายฟรี)
genai.configure(api_key=api_key)
model = genai.GenerativeModel('gemini-flash-latest')

# --- Sidebar เมนูด้านซ้าย ---
with st.sidebar:
    st.title("🎬 Smart Creator Hub")
    st.write(f"สวัสดีค่ะคุณเก่ง")
    menu = st.radio(
        "เลือกเครื่องมือที่ต้องการ:",
        [
            "🎬 คิดสคริปต์คลิป", 
            "💰 เขียนแคปชั่นป้ายยา", 
            "🔍 ตั้งชื่อคลิปให้น่าคลิก", 
            "💬 ผู้ช่วยตอบคอมเมนต์"
        ]
    )
    st.divider()
    st.caption("Smart Creator Hub v1.2 | Powered by Gemini AI")

# --- โซน 1: คิดสคริปต์คลิป ---
if menu == "🎬 คิดสคริปต์คลิป":
    st.header("🎬 ผู้ช่วยคิดสคริปต์คลิปสั้น (TikTok/Reels)")
    product_name = st.text_input("สินค้าหรือหัวข้อที่ต้องการทำคลิป", placeholder="เช่น ครีมกันแดด, รีวิวหมูกระทะ")
    style = st.selectbox("อยากได้โทนคลิปแบบไหน?", ["ตลก/กวนๆ", "ดราม่า/เล่าเรื่อง", "จริงจัง/น่าเชื่อถือ", "ASMR/ผ่อนคลาย"])
    
    if st.button("✨ เสกสคริปต์"):
        if not product_name:
            st.warning("กรุณาใส่ชื่อสินค้าหรือหัวข้อก่อนนะคะ")
        else:
            with st.spinner("AI กำลังเขียนบทให้ค่ะ..."):
                prompt = f"""
                ช่วยคิดสคริปต์ TikTok/Reels ความยาว 15-30 วินาที
                สำหรับ: "{product_name}"
                สไตล์: {style}
                ขอโครงสร้าง:
                1. HOOK (3 วินาทีแรกที่ต้องหยุดคนดูให้ได้)
                2. เนื้อหา (แบ่งเป็นฉากๆ พร้อมสิ่งที่ต้องถ่าย)
                3. CTA (ตอนจบกระตุ้นให้กดติดตามหรือกดลงตะกร้า)
                ขอภาษาไทยที่เป็นธรรมชาติ วัยรุ่นชอบ
                """
                try:
                    res = model.generate_content(prompt)
                    st.success("สคริปต์พร้อมแล้วค่ะ! 👇")
                    st.markdown(res.text)
                except Exception as e:
                    st.error(f"เกิดข้อผิดพลาด: {e}")

# --- โซน 2: เขียนแคปชั่นป้ายยา ---
elif menu == "💰 เขียนแคปชั่นป้ายยา":
    st.header("💰 เขียนแคปชั่นป้ายยา Affiliate")
    details = st.text_area("ก๊อปชื่อสินค้าและจุดเด่นมาวางตรงนี้", height=150, placeholder="เช่น หูฟังบลูทูธ แบตอึด 40 ชม. กันน้ำ เบสแน่น")
    platform = st.selectbox("เลือกแพลตฟอร์มที่จะไปโพสต์", ["Facebook", "TikTok (แคปชั่นใต้คลิป)", "Twitter (X)", "Instagram"])
    
    if st.button("💸 เสกแคปชั่น"):
        if not details:
            st.warning("กรุณาใส่ข้อมูลสินค้าก่อนนะคะ")
        else:
            with st.spinner("กำลังเรียบเรียงคำพูดให้คนอยากซื้อ..."):
                prompt = f"""
                เขียนแคปชั่นขายของ (Sales Copy) สำหรับโพสต์ลง {platform}
                ข้อมูลสินค้า: {details}
                โจทย์: เน้นการป้ายยาแรงๆ แก้ปัญหาให้คนอ่าน และกระตุ้นให้กดลิงก์ทันที ใส่ Emoji และ Hashtag ที่เกี่ยวข้อง
                """
                try:
                    res = model.generate_content(prompt)
                    st.success("ก๊อปไปโพสต์ได้เลยค่ะ! 👇")
                    st.code(res.text, language=None)
                except Exception as e:
                    st.error(f"เกิดข้อผิดพลาด: {e}")

# --- โซน 3: ตั้งชื่อคลิปให้น่าคลิก ---
elif menu == "🔍 ตั้งชื่อคลิปให้น่าคลิก":
    st.header("🔍 ตัวช่วยตั้งชื่อคลิป (Viral Title Generator)")
    topic = st.text_input("คลิปของคุณเกี่ยวกับเรื่องอะไร?", placeholder="เช่น วิธีซ่อมหน้าจอไอโฟน, พากินร้านลับ")
    
    if st.button("🚀 เสกชื่อคลิป"):
        if not topic:
            st.warning("กรุณาบอกหัวข้อคลิปก่อนนะคะ")
        else:
            with st.spinner("กำลังคิดชื่อที่คนต้องเหลียวมอง..."):
                prompt = f"ช่วยคิดชื่อคลิปที่น่าสนใจและอยากให้คนกดดู (Viral Title) สำหรับเรื่อง '{topic}' มา 5 แบบ ทั้งแบบที่ดูตื่นเต้นและแบบที่ดูมีความรู้"
                try:
                    res = model.generate_content(topic)
                    st.success("ลองเลือกชื่อที่ชอบดูนะคะ:")
                    st.markdown(res.text)
                except Exception as e:
                    st.error(f"เกิดข้อผิดพลาด: {e}")

# --- โซน 4: ผู้ช่วยตอบคอมเมนต์ ---
elif menu == "💬 ผู้ช่วยตอบคอมเมนต์":
    st.header("💬 ผู้ช่วยตอบคอมเมนต์เพิ่มยอดแชร์")
    user_comment = st.text_area("ก๊อปคอมเมนต์จากแฟนคลับมาวางตรงนี้", placeholder="เช่น สนใจครับ, ราคาเท่าไหร่, พิกัดหน่อย")
    reply_style = st.select_slider("อยากตอบสไตล์ไหน?", options=["สุภาพนอบน้อม", "เป็นกันเอง/พี่น้อง", "กวนประสาทแบบฮาๆ"])
    
    if st.button("💭 คิดคำตอบ"):
        if not user_comment:
            st.warning("กรุณาวางคอมเมนต์ก่อนนะคะ")
        else:
            with st.spinner("กำลังหาคำตอบที่โดนใจ..."):
                prompt = f"ช่วยคิดคำตอบสำหรับคอมเมนต์: '{user_comment}' โดยตอบในสไตล์ {reply_style} เพื่อให้เกิดการคุยต่อและเพิ่มค่าการมองเห็น"
                try:
                    res = model.generate_content(prompt)
                    st.success("คำตอบที่แนะนำค่ะ:")
                    st.code(res.text, language=None)
                except Exception as e:
                    st.error(f"เกิดข้อผิดพลาด: {e}")