import streamlit as st
import os
from dotenv import load_dotenv
import google.generativeai as genai

# 1. ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="Smart Creator Hub", page_icon="🎬", layout="wide")

# 2. โหลดกุญแจ AI
load_dotenv()
api_key = os.getenv("GEMINI_API_KEY")

if not api_key:
    st.error("❌ ไม่พบ API Key! กรุณาตรวจสอบใน Secrets นะคะ")
    st.stop()

genai.configure(api_key=api_key)
model = genai.GenerativeModel('gemini-flash-latest')

# --- Sidebar เมนูด้านซ้าย ---
with st.sidebar:
    st.title("🎬 Smart Creator Hub")
    st.write("ยินดีต้อนรับค่ะคุณเก่ง")
    menu = st.radio(
        "เลือกเครื่องมือที่ต้องการ:",
        [
            "🎬 วางแผนคอนเทนต์ (ทุกแพลตฟอร์ม)", 
            "💰 เขียนแคปชั่นป้ายยา", 
            "🔍 ตั้งชื่อคลิปให้น่าคลิก", 
            "💬 ผู้ช่วยตอบคอมเมนต์"
        ]
    )
    st.divider()
    st.caption("Smart Creator Hub v1.3 | All-in-One Creator Tools")

# --- โซน 1: วางแผนคอนเทนต์ (อัปเกรดใหม่!) ---
if menu == "🎬 วางแผนคอนเทนต์ (ทุกแพลตฟอร์ม)":
    st.header("🎬 ผู้ช่วยวางแผนและเขียนบทคอนเทนต์")
    
    col1, col2 = st.columns(2)
    with col1:
        topic = st.text_input("หัวข้อคอนเทนต์", placeholder="เช่น รีวิวสินค้า, สอนเทคนิคต่างๆ, เล่าประสบการณ์")
        platform = st.selectbox("ต้องการลงช่องทางไหน?", [
            "วิดีโอสั้น (TikTok / Reels / Shorts)",
            "วิดีโอยาว (Facebook Page / YouTube)",
            "โพสต์รูปภาพ / บทความ (Facebook / IG)",
            "Live สด (ร่างสคริปต์หัวข้อพูดคุย)"
        ])
    with col2:
        target = st.text_input("กลุ่มเป้าหมาย", placeholder="เช่น ลูกค้าทั่วไป, ช่างเทคนิค, วัยรุ่น")
        mood = st.selectbox("อารมณ์ของคอนเทนต์", ["ตลก/สนุกสนาน", "สาระแน่นๆ", "แรงบันดาลใจ", "ป้ายยาของดี"])

    if st.button("✨ เสกคอนเทนต์"):
        if not topic:
            st.warning("กรุณาใส่หัวข้อก่อนนะคะ")
        else:
            with st.spinner("กำลังวางแผนคอนเทนต์ให้รอบด้าน..."):
                prompt = f"""
                ในฐานะผู้เชี่ยวชาญด้าน Content Strategy ช่วยวางแผนคอนเทนต์สำหรับ:
                หัวข้อ: "{topic}"
                แพลตฟอร์ม: {platform}
                กลุ่มเป้าหมาย: {target}
                อารมณ์/โทน: {mood}
                
                สิ่งที่ต้องการ:
                1. ชื่อเรื่องที่ดึงดูด (Headline)
                2. โครงสร้างเนื้อหา (ถ้าเป็นวิดีโอให้บอกฉากและคำพูด, ถ้าเป็นโพสต์ให้เขียนเนื้อหาที่น่าอ่าน)
                3. จุดเด่นที่ต้องเน้น (Key Message)
                4. คำแนะนำเพิ่มเติมเพื่อให้เป็น Viral
                """
                try:
                    res = model.generate_content(prompt)
                    st.success("แผนคอนเทนต์ของคุณพร้อมแล้วค่ะ! 👇")
                    st.markdown(res.text)
                except Exception as e:
                    st.error(f"เกิดข้อผิดพลาด: {e}")

# --- โซน 2: เขียนแคปชั่นป้ายยา ---
elif menu == "💰 เขียนแคปชั่นป้ายยา":
    st.header("💰 เขียนแคปชั่นป้ายยา Affiliate")
    details = st.text_area("ข้อมูลสินค้า/จุดเด่น", height=150)
    if st.button("💸 เสกแคปชั่น"):
        prompt = f"เขียนแคปชั่นขายของป้ายยาแรงๆ สำหรับ {details} ใส่ Emoji และ Hashtag"
        res = model.generate_content(prompt)
        st.code(res.text, language=None)

# --- โซน 3: ตั้งชื่อคลิปให้น่าคลิก ---
elif menu == "🔍 ตั้งชื่อคลิปให้น่าคลิก":
    st.header("🔍 ตัวช่วยตั้งชื่อให้คนกดดู")
    topic_name = st.text_input("เนื้อหาคลิปโดยสรุป")
    if st.button("🚀 เสกชื่อคลิป"):
        prompt = f"คิดชื่อคลิป Viral 5 แบบ สำหรับเรื่อง '{topic_name}'"
        res = model.generate_content(topic_name)
        st.markdown(res.text)

# --- โซน 4: ผู้ช่วยตอบคอมเมนต์ ---
elif menu == "💬 ผู้ช่วยตอบคอมเมนต์":
    st.header("💬 ผู้ช่วยตอบคอมเมนต์")
    comment = st.text_area("ข้อความจากแฟนคลับ")
    style = st.select_slider("สไตล์การตอบ", options=["สุภาพ", "เป็นกันเอง", "กวนๆ"])
    if st.button("💭 คิดคำตอบ"):
        prompt = f"คิดคำตอบสำหรับคอมเมนต์ '{comment}' ในสไตล์ {style}"
        res = model.generate_content(prompt)
        st.code(res.text, language=None)